<h1>My task</h1>
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p>
  <% if @task.completed %>
    This task has been completed
  <% else %>
    This task remains to be done
  <% end %>
</p>
<%= link_to 'Back to list', tasks_path %>
<%= link_to 'Edit task', edit_path(@task) %>
